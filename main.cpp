#include <iostream>
#include <fstream>
#include <string>
#include <cstdlib>
#include <vector>
#include <sstream>
#include "BestSeller.h"
#include "BestSellerList.h"
using namespace std;

/*
 *
 * For testing, you should call each of these functions several times in the main program with “hard coded” parameters,
 * and compare the results to the input file to verify that they are working properly.
 * Include your program output in your project report to demonstrate your program correctness.
 */

/*
 * To test the four class methods above you will need to open one of the NYT best sellers files,
 * and loop over this file to read and insert best seller information into a BestSellerList.
 * Then you can call these methods above several times with “hard coded” parameters to demonstrate that your program is answering these four questions properly.
 * Students are not required to create an interactive interface for users to enter any sequence of questions.
 */



int main()
{


    BestSellerList bestSellerList;
    //Testing = new BestSellerNode(new BestSellerNode(2019, "01/05/2019", 1, 9780735219090, "WHERE THE CRAWDADS SING", "Delia Owens", "Putnam", 26, 0));

    //bestSellerList.InsertHead(new BestSellerNode(new BestSellerNode(2019, "01/05/2019", 1, 9780735219090, "WHERE THE CRAWDADS SING", "Delia Owens", "Putnam", 26, 0)));
    return 0;

}